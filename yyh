Real Cost of Max Speed Project = 
CALCULATE(
    MIN(Table1[Real cost (2023$)]),
    FILTER(
        ALLEXCEPT(Table1, Table1[Country], Table1[Line]),
        Table1[Max speed] = MAXX(
            ALLEXCEPT(Table1, Table1[Country], Table1[Line]),
            Table1[Max speed]
        )
    )
)

Line by Real Cost = 
CALCULATE(
    CONCATENATEX(
        FILTER(
            ALLEXCEPT(Table1, Table1[Country], Table1[Line]),
            Table1[Max speed] = MAXX(
                ALLEXCEPT(Table1, Table1[Country], Table1[Line]),
                Table1[Max speed]
            )
        ),
        Table1[Line],
        " | "
    )
)


Max Speed Year = 
VAR MaxSpeed = 
    CALCULATE(
        MAX('high speed rail'[Max speed]), 
        ALLEXCEPT('high speed rail', 'high speed rail'[Country], 'high speed rail'[Line])
    )
RETURN
    CALCULATE(
        MAX('high speed rail'[Year]),
        FILTER(
            'high speed rail',
            'high speed rail'[Max speed] = MaxSpeed &&
            'high speed rail'[Country] = EARLIER('high speed rail'[Country]) &&
            'high speed rail'[Line] = EARLIER('high speed rail'[Line])
        )
    )


